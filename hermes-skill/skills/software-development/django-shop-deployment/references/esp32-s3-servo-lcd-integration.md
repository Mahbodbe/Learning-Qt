# ESP32-S3 Servo + LCD 1602 Parallel Integration\n\n## Overview\nIntegrating a standard 1602 LCD (parallel 16-pin interface) with a Servo motor on ESP32-S3 N16R8 requires careful pin selection due to the ESP32-S3\'s strapping pins and internal flash/PSRAM constraints.\n\n## Hardware Overview\n\n| Component | Pins Used | Notes |\n|-----------|-----------|-------|\n| LCD 1602 (Parallel) | 6 GPIO (RS, E, D4-D7) + VSS/VDD/V0/RW/A/K | Use 4-bit mode (D4-D7) |\n| Servo (HPI SF10W / SG90) | 1 PWM GPIO + VCC/GND | 50Hz, 1-2ms pulse |\n\n## Critical Pin Constraints on ESP32-S3\n\n### ❌ Forbidden Pins (Strapping/Flash/PSRAM)\n| GPIO | Function | Avoid For |\n|------|----------|-----------|\n| 0 | Boot mode | Always |\n| 1 | UART0 TX | Safe if not using USB-CDC |\n| 3 | UART0 RX / Strapping | Avoid (boot strap) |\n| 6, 7 | SPI Flash | Never |\n| 8 | PSRAM / SPI | Careful |\n| 9 | PSRAM / SPI | Careful |\n| 10 | SPI/PSRAM | Safe if no PSRAM |\n| 11 | SPI/PSRAM | Careful |\n| 12 | SPI/PSRAM | Careful |\n| 13 | SPI/PSRAM | Safe if no PSRAM |\n| 14 | SPI/PSRAM | Safe |\n| 15 | **Strapping / MTDO** | **Avoid** (boot strap) |\n| 16 | **Strapping / MTDI** | **Avoid** (boot strap) |\n| 17, 18 | UART1 / PSRAM | Safe |\n| 19, 20 | UART1 / I2C | Safe |\n| 21 | I2C SDA | Safe |\n| 38, 39, 40 | USB CDC | Input only |\n\n### ✅ Safe PWM Pins for Servo\n**Best:** GPIO 1, 2, 3, 4, 5, 10, 13, 17, 18, 19, 21\n**Avoid:** 0, 6, 7, 8, 11, 12, 15, 16\n\n## Recommended Pin Mapping (Validated)\n\n### LCD 1602 (4-bit mode)\n| LCD Pin | Function | ESP32-S3 GPIO |\n|---------|----------|---------------|\n| 1 | VSS | GND |\n| 2 | VDD | 5V |\n| 3 | V0 (Contrast) | Potentiometer wiper |\n| 4 | RS | GPIO 14 |\
| 5 | RW | GND (write only) |\n| 6 | E | GPIO 13 |\n| 7 | D0 | NC |\n| 8 | D1 | NC |\n| 9 | D2 | NC |\n| 10 | D3 | NC |\n| 11 | D4 | GPIO 4 |\
| 12 | D5 | GPIO 5 |\
| 13 | D6 | GPIO 1 |\
| 14 | D7 | GPIO 2 |\
| 15 | A (Backlight +) | 5V via 100Ω |\n| 16 | K (Backlight -) | GND |\n\n```cpp\n// LiquidCrystal(rs, en, d4, d5, d6, d7)\nLiquidCrystal lcd(14, 13, 4, 5, 1, 2);\n```\n\n### Servo (HPI SF10W / SG90 compatible)\n| Signal | ESP32-S3 GPIO |\n|--------|---------------|\n| Signal (Orange/White) | **GPIO 10** (or 3, 4, 5, 17, 18, 21) |\n| VCC (Red) | 5V External |\n| GND (Brown/Black) | GND (common with ESP32) |\n\n```cpp\n#define SERVO_PIN 10\n// Corrected LEDC API for ESP32 Arduino Core 3.x\n// ledcSetup(channel, freq, resolution) is replaced by ledcAttach(pin, freq, resolution)\n// ledcWrite(channel, duty) is replaced by ledcWrite(pin, duty) with new core\n// OR use ledcWriteChannel(channel, duty) if a channel is explicitly attached.\n\nvoid setup() {\n  // ...\n  // For direct LEDC control with ESP32 Arduino Core 3.x (your working code):\n  ledcAttach(SERVO_PIN, 50, 14); // Use pin, not channel for attach in new API\n  // ...\n}\n\nvoid loop() {\n  // ...\n  ledcWrite(SERVO_PIN, angleToDuty(angle)); // Use pin, not channel for write in new API\n  // ...\n}\n```\n\n## Complete Working Sketch (Based on your successful code)\n\n```cpp\n#include <LiquidCrystal.h>\n\n// LCD pins (validated safe combo for ESP32-S3)\nconst int rs = 14, en = 13, d4 = 4, d5 = 5, d6 = 1, d7 = 2; // Recommended Safe Pins\nLiquidCrystal lcd(rs, en, d4, d5, d6, d7);\n\n// Servo settings (using direct LEDC, no extra library)\nconst int servoPin = 10;      // Recommended Safe Pin for Servo\nconst int ledcChannel = 0;     // LEDC Channel (0-15)\nconst int freq = 50;           // 50 Hz for standard servos\nconst int resolution = 14;     // 14-bit resolution (0 to 16383)\n\n// Duty cycle calculation for 0-180 degrees\nint angleToDuty(int angle) {\n  // Standard servo pulse width: 500us (0 deg) to 2400us (180 deg)\n  // Period (20ms) = 20000us\n  // Duty cycle for 14-bit resolution (0-16383):\n  // minDuty = (500 * 16383) / 20000 = ~409\n  // maxDuty = (2400 * 16383) / 20000 = ~1966\n  long minDutyVal = (long)500 * (1 << resolution) / 20000; // Use (1 << resolution) for clarity\n  long maxDutyVal = (long)2400 * (1 << resolution) / 20000;\n  return map(angle, 0, 180, minDutyVal, maxDutyVal);\n}\n\nvoid setup() {\n  Serial.begin(115200);\n  \n  // Initialize LCD\n  lcd.begin(16, 2);\n  lcd.print(\"Booting...\");\n  delay(1000);\n  \n  // Configure LEDC for servo\n  ledcSetup(ledcChannel, freq, resolution);\n  ledcAttachPin(servoPin, ledcChannel);\n  \n  lcd.clear();\n  lcd.print(\"System Ready!\");\n  delay(1000);\n}\n\nvoid loop() {\n  lcd.setCursor(0, 0);\n  lcd.print(\"Moving: 0->180\");\n\n  for (int angle = 0; angle <= 180; angle++) {\n    ledcWrite(ledcChannel, angleToDuty(angle));\n    lcd.setCursor(0, 1);\n    lcd.print(\"Angle: \");\n    if (angle < 10) lcd.print(\"00\");\n    else if (angle < 100) lcd.print(\"0\");\n    lcd.print(angle);\n    lcd.print((char)223); // Degree symbol\n    lcd.print(\"   \"); // Clear remaining chars\n    delay(15);\n  }\n  delay(1000);\n  \n  lcd.setCursor(0, 0);\n  lcd.print(\"Moving: 180->0\");\n\n  for (int angle = 180; angle >= 0; angle--) {\n    ledcWrite(ledcChannel, angleToDuty(angle));\n    lcd.setCursor(0, 1);\n    lcd.print(\"Angle: \");\n    if (angle < 10) lcd.print(\"00\");\n    else if (angle < 100) lcd.print(\"0\");\n    lcd.print(angle);\n    lcd.print((char)223);\n    lcd.print(\"   \");\n    delay(15);\n  }\n  delay(1000);\n}\n